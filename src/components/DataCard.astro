---
import classNames from "classnames";

interface Props {
    title: string;
    className?: string;
}

const { title, className } = Astro.props;
---

<section class={classNames("mb-5", className)}>
    <h3 class="text-3xl mb-3">{title}</h3>
    <slot />
</section>
